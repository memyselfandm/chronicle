# Supabase Configuration
# Copy this file to .env.local and fill in your actual values
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Optional: Database URL for direct connections (if needed)
DATABASE_URL=your_postgres_connection_string

# Optional: Service Role Key for server-side operations (keep secret)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Development Configuration
NODE_ENV=development

# Optional: Analytics Configuration (for future use)
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id_here

# Optional: Sentry Configuration (for future use)
# SENTRY_DSN=your_sentry_dsn_here